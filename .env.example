# .env.example
# Copie este arquivo para .env e preencha com seus valores
# cp .env.example .env

# ===== CONFIGURAÇÃO OBRIGATÓRIA =====

# Credenciais do PostgreSQL (servidor externo)
PG_USER=your_postgres_username
PG_PASSWORD=your_postgres_password

# Senha do Email para alertas (use App Password para Gmail)
EMAIL_PASSWORD=your_email_app_password

# ===== CONFIGURAÇÃO OPCIONAL =====

# Integração com Slack (opcional)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Webhook customizado (opcional)
# WEBHOOK_URL=https://your-webhook-endpoint.com/alerts

# ===== NOTAS IMPORTANTES =====
# 1. PostgreSQL deve estar instalado EXTERNAMENTE no servidor
# 2. Configure o host/porta do PostgreSQL em: config/pg_monitor_config.yml
# 3. Para Gmail, use "App Password" ao invés da senha normal
# 4. Nunca commite o arquivo .env no Git (já está no .gitignore)
